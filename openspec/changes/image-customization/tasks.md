## 1. Base image: add ccstatusline

- [x] 1.1 Edit `internal/image/Dockerfile` — add `ccstatusline` to the existing `npm install -g` line alongside `@anthropic-ai/claude-code`

## 2. internal/config package

- [x] 2.1 Create `internal/config/config.go` — define `Config`, `ImageConfig`, `ExtraStep` structs; implement `Load(projectPath string) (*Config, error)` using Viper to read `sandbox.yaml`; return empty `Config{}` (not an error) when no `sandbox.yaml` is present
- [x] 2.2 Add unit tests `internal/config/config_test.go` — test `Load` with a valid `sandbox.yaml`, with a missing file, and with an `image.extra` block; use table-driven tests and Testify

## 3. Extension image in internal/image

- [x] 3.1 Add `ExtImageName(project string) string` to `internal/image/image.go` — returns `claustro-{project}:latest`
- [x] 3.2 Add `extHash(steps []string) string` — SHA-256 of the joined step strings; used as a change-detection label
- [x] 3.3 Add `EnsureExtended(ctx, cli, project string, steps []string, w io.Writer) error` — checks if the extension image exists **and** its `claustro.ext-hash` label matches `extHash(steps)`; if not, calls `buildExtended`
- [x] 3.4 Add `buildExtended(ctx, cli, project string, steps []string, w io.Writer) error` — generates a `FROM claustro:latest` + `RUN` Dockerfile in memory, builds it as `claustro-{project}:latest` via the Docker SDK, sets `claustro.ext-hash` label
- [x] 3.5 Add unit tests `internal/image/image_test.go` — test `ExtImageName`, `extHash` (determinism, order-sensitivity); test `buildExtended` Dockerfile generation (verify generated bytes without calling Docker)

## 4. Plumb config into up command

- [x] 4.1 Edit `cmd/claustro/up.go` — call `config.Load(id.HostPath)` after resolving identity; if `config.Image.Extra` is non-empty, call `image.EnsureExtended` instead of `image.EnsureBuilt`, and pass `image.ExtImageName(id.Project)` as the container image override to `container.Create`
- [x] 4.2 Edit `internal/container/container.go` — add an `ImageName string` field to a new `CreateOptions` struct (or add an `imageName string` parameter to `Create`) so the caller can override the image used; default to `image.ImageName` if empty
- [x] 4.3 Add unit tests `cmd/claustro/commands_test.go` — test that `newUpCmd` still registers correctly; extend to verify config-loading path compiles (no behavioral test needed here since Docker calls require integration)

## 5. Rebuild: also rebuild extension image

- [x] 5.1 Edit `cmd/claustro/rebuild.go` — after rebuilding the base image, check if a `claustro-{project}:latest` extension image exists; if so, call `image.BuildExtended` to force-rebuild it; add a `--no-ext` flag to skip this step

## 6. Verify

- [x] 6.1 `go build ./...` passes with no errors
- [x] 6.2 `go test ./...` passes (all new unit tests green)
- [x] 6.3 `golangci-lint run` passes with no new warnings
- [x] 6.4 Manual smoke test: `claustro up` with no `sandbox.yaml` behaves identically to before
- [x] 6.5 Manual smoke test: `claustro up` with a `sandbox.yaml` containing `image.extra` builds `claustro-{project}:latest` and uses it for the container
