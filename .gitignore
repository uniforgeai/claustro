# Build output
bin/
dist/
*.exe

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
*.env
.env.local

# Go
vendor/
*.test
coverage.out
coverage.html

# Audio bridge shim binaries (built by make build-shims, embedded via go:embed)
internal/image/rec-shim
internal/image/arecord-shim

# Stray binaries in project root (from go build without -o)
/claustro
/rec-shim
/arecord-shim

# macOS
.DS_Store

# Claude Code local settings (user-specific, not shared)
.claude/settings.local.json

# Git worktree metadata (worktree dirs live outside the repo)
.claude/worktrees/
